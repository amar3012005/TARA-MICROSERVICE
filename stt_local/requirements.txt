# STT Local Microservice Dependencies - Lightweight (Build Time)
# Heavy packages are in requirements_after.txt and installed manually in container

# FastAPI and server
fastapi>=0.115.0
uvicorn[standard]>=0.24.0
websockets>=13.0.0
python-multipart>=0.0.6

# FastRTC for browser audio streaming
fastrtc>=0.0.15

# Pydantic for data validation
pydantic>=2.5.0

# Audio processing (lightweight)
numpy==1.24.3
scipy>=1.11.0

# Redis client (optional, for state persistence)
redis[asyncio]>=5.0.0

# HTTP client for health checks
httpx>=0.24.0

# Environment variables
python-dotenv==1.0.1

# Testing dependencies (optional)
pytest>=7.4.0
pytest-asyncio>=0.21.0
