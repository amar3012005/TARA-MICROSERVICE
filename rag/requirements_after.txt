# HEAVY PACKAGES - Install manually after container is built
# Run: pip install --default-timeout=1000 --user --no-cache-dir -r requirements_after.txt

# Sentence transformers and ML dependencies (HEAVY - ~2GB+)
sentence-transformers>=2.2.0
langchain-huggingface>=0.0.1
huggingface-hub>=0.16.0
torch>=2.0.0

# Note: These will be installed automatically as dependencies:
# - torch (via sentence-transformers)
# - tokenizers (via sentence-transformers)
# - transformers (via sentence-transformers)
# - nvidia_cublas_cu12 (if GPU available, via torch)
